
/**
 * @deprecated This file is kept for backward compatibility.
 * Please import from src/utils/executableCode/index.ts directly in new code.
 */

import { 
  mainExecutableCode,
  emulatorExeCode,
  setupExeCode,
  allExecutableCodes
} from './executableCode';

// Re-export everything from the new structure
export {
  mainExecutableCode,
  emulatorExeCode,
  setupExeCode,
  allExecutableCodes
};
